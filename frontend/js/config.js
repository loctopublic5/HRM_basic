const CONFIG = {
    // Đường dẫn đến file api.php của Backend
    // LƯU Ý: Hãy thay đổi localhost thành đường dẫn thực tế của bạn nếu cần
    API_URL: 'http://localhost/hrm-project/backend/api.php',
    
    // Các hằng số khác (nếu có)
    DATE_LOCALE: 'vi-VN',
    CURRENCY_LOCALE: 'vi-VN',
    CURRENCY: 'VND'
};

export default CONFIG;